import 'package:flutter/material.dart';

class InsertOwnerCard extends StatelessWidget {
  const InsertOwnerCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
